Instructions to start program:

In one terminal start the backend:
cd \backend\
uvicorn app.main:app --reload

In another terminal start the frontend:
cd \frontend\
npm run dev

Access website through localhost:5173 in a browser